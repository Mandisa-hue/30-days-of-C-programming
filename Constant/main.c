#include <stdio.h>
#include <stdlib.h>

int main()
{
    /* to enable it not to change you use const and Capital letters */
    const int NUM = 5;
    printf("%d\n", num);

    /* to modify the number variable to 8 */
    num = 8;
    printf("%d", num);

    /* a constant in text form because it doesn't change*/
    printf("Hello")

    /* a constant using numbers 20 remains the same */
    printf("%d/n", 20)
    return 0;
}
